import { Bell, ChevronDown, LogOut, Search } from "lucide-react";
import { useState } from "react";
import { useAuth } from "../../context/AuthContext";
import { MobileMenuButton } from "./Sidebar";
export default function Navbar({ onMenu }) { const { user, logout } = useAuth(); const [menu, setMenu] = useState(false); const initials = (user?.username || user?.email || "C").slice(0, 1).toUpperCase(); return <header className="sticky top-0 z-20 flex h-20 items-center gap-3 border-b border-slate-100 bg-white/90 px-5 backdrop-blur lg:px-8"><MobileMenuButton onClick={onMenu}/><div className="hidden max-w-md flex-1 md:block"><div className="flex items-center gap-2 rounded-xl bg-slate-50 px-3 py-2.5 text-slate-400"><Search size={18}/><input aria-label="Global search" placeholder="Search inventory..." className="w-full bg-transparent text-sm text-slate-700 outline-none" /></div></div><div className="ml-auto flex items-center gap-3"><button aria-label="Notifications" className="relative rounded-xl p-2.5 text-slate-500 hover:bg-slate-50"><Bell size={20}/><span className="absolute right-2 top-2 h-2 w-2 rounded-full bg-blue-600 ring-2 ring-white"/></button><div className="relative"><button onClick={() => setMenu(!menu)} className="flex items-center gap-2 rounded-xl p-1.5 hover:bg-slate-50"><span className="grid h-9 w-9 place-items-center rounded-full bg-blue-100 text-sm font-bold text-blue-700">{initials}</span><span className="hidden text-left sm:block"><b className="block max-w-32 truncate text-sm text-slate-800">{user?.username || "My account"}</b><small className="capitalize text-slate-500">{user?.role || "customer"}</small></span><ChevronDown size={16} className="text-slate-400"/></button>{menu && <div className="absolute right-0 mt-2 w-44 rounded-xl border border-slate-100 bg-white p-1 shadow-lg"><button onClick={logout} className="flex w-full items-center gap-2 rounded-lg px-3 py-2 text-sm text-rose-600 hover:bg-rose-50"><LogOut size={16}/>Sign out</button></div>}</div></div></header>; }